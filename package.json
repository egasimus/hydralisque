{
  "name": "hydralisque",
  "version": "0.0.0",
  "devDependencies": {
    "lerna": "^3.22.1",
    "lerna-wizard": "^1.1.1"
  },
  "scripts": {
    "start": "electron electron/app.js",
    "shell": "nix-shell",
    "build": "nix-build -E 'with import <nixpkgs> {}; callPackage ./default.nix {}'"
  },
  "private": true,
  "workspaces": [
    "atom",
    "editor",
    "electron",
    "server",
    "synth"
  ]
}
